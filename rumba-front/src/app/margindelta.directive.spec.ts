import { MargindeltaDirective } from './margindelta.directive';

describe('MargindeltaDirective', () => {
  it('should create an instance', () => {
    const directive = new MargindeltaDirective();
    expect(directive).toBeTruthy();
  });
});
